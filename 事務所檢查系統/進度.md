# 進度與待辦（隨版本更新）

## 版本
- v1.2（2026-09-13）Mac 環境修正（.venv）；選案改以永續為主軸；網頁表單以 clasp 實際部署並修正匯入；收錄前置程式。
- v1.1（2026-09-10）新增 macOS 一鍵執行 .command（專案控管機為 2017 MacBook Air）。
- v1.0（2026-09-10）選案模組＋個案檢查表輸入／產出＋網頁表單原始碼＋缺失申報站 Apps Script，初版入庫。

## 狀態
| 模組 | 狀態 | 備註 |
|---|---|---|
| 0 Mac 環境 | 可用（Python 3.11.16 via uv，專案內 .venv） | `_env.sh` 改為自動建 .venv 裝套件，避開 PEP 668 外部管理環境限制 |
| 1 選案（bin/select_cases.py） | 可用，115 安永實跑 | 09/13 依長官指示改永續主軸：確信重疊加分 10→20、台塑化(6505)指定納入；名單見 參考/選案規則說明 |
| 2 網頁表單（apps_script/） | **已部署（測試帳號 dinefour@gmail.com）**，匯入 15 案已由 API 驗證 | 用 clasp 部署，網址見下；存取設為任何人不必登入；第一次開表單自動 setup；`?action=state/import` 診斷介面 |
| 2' Excel 輸入檔（2_建立檢查輸入檔） | 可用 | 無表單時的備案 |
| 3 產出（bin/render_case_docs.py） | 可用，115 安永 15 案已產出五種檔 | 檢查表二為重建版面 |
| 缺失申報站（apps_script_缺失申報站/） | 程式完成 | 尚未部署；與表單「同步缺失」對接 |
| 前置程式（前置程式/） | 已收錄 選案清單 1 支＋永續確信比對 7 支＋SOP | 皆為 Windows 路徑；名冊擷取程式仍未找到 |

## 網頁表單（測試部署，2026-09-13）
- 試算表：https://docs.google.com/spreadsheets/d/1UYGCephaKqVqP_WHxTnWLWPP0lkKnoItWw0NGldpCDE
- Apps Script：https://script.google.com/d/16rRFiEemBNWMRH7i8wUNMFIRAFzUSIuWXM9hXErhWPS0QF_1wGf2sady/edit
- 網頁表單：https://script.google.com/macros/s/AKfycbyN90aSQU33YMnS8jLEBcn5egjeqrH02blYkAGXbQjK2Ee_mE_Mh7hijQTflL6hyJBN/exec
- 部署方式：Mac 上 `cd apps_script && clasp push -f && clasp update-deployment -d "說明" <deploymentId>`（clasp 已登入 dinefour；`.clasp.json` 不進版）
- 確認順了再以 accsfb01 重做一次 `clasp login` → `clasp create-script --type sheets` → push → deploy。

## 待辦
- [ ] **表單匯入 UI 實測**：重整表單 → 設定／匯出 → 貼「建議選案」→ 匯入，應顯示「已匯入 N 案」（伺服器端已驗證；前端改為先解析再送、run() 綁定 this，待瀏覽器確認）；接著實測填答、缺失、匯出 xlsx → 3_產出檢查表
- [ ] 表單「人員」工作表填同仁姓名（填表人下拉來源）
- [ ] 確認後改用 accsfb01 正式部署
- [ ] 找到或重寫「四大會計師事務所會計師名冊」擷取程式，放進 前置程式/
- [ ] 前置程式改為相對路徑／參數化，讓 Mac 也能跑（或明確標示僅 Windows）
- [ ] 取得 114 年度實質審閱名單 → 填入 input/選案參數 → 重跑 1_選案 → 目標家數改 8
- [ ] 長官若不希望台塑化擠掉既有個案：參數表「台北所上限」改 8 或「目標家數」改 16
- [ ] 檢查表二版面比對原檔；若取得 .docx 改為套原檔
- [ ] 總結會議紀錄範本到位後加入產出
- [ ] 檢查表一底稿版（8 表、查核情形欄）由作答帶入
- [ ] AML 客戶審查抽樣表（表 8、9）輸入設計
- [ ] 英文問卷 E73 改 0、Part IV D22 刪 [specify here]；繁體版重新上傳 Drive
